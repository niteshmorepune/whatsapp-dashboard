{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git commit *)",
            "command": "cd 'D:/Projects/Whatsapp Dashboard' && if npx tsc --noEmit && npm run lint; then exit 0; else echo '{\"continue\":false,\"stopReason\":\"TypeScript or ESLint errors found. Fix them before committing.\"}'; exit 1; fi",
            "timeout": 90,
            "statusMessage": "Checking TypeScript + ESLint before commit..."
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git push *)",
            "command": "echo '{\"systemMessage\":\"Push done. Deploy on VPS: cd /opt/app/whatsapp-dashboard && git pull && docker compose up -d --build\"}'",
            "timeout": 10,
            "statusMessage": "Showing deploy reminder..."
          }
        ]
      }
    ]
  }
}
